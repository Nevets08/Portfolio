const content = document.querySelector('#menu');
const insertTarget = document.querySelector('#Apropods');
scrollnav.init(content, { 
  debug: false,
  insertTarget: insertTarget,
  insertLocation: 'append'
});